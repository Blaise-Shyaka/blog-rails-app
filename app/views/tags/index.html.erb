<h1>List of all tags</h1>

<ul>
  <% @tags.each do |tag| %>
    <li><%= link_to tag.name, tag_path(tag) %> 
    <%= link_to "Delete Tag", tag_path(tag), method: :delete, data: {confirm: "Really delete the Tag?"} %></li>
  <% end %>
</ul>
